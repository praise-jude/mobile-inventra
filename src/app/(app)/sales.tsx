import { ComingSoon } from '@/components/coming-soon';

export default function SalesScreen() {
  return (
    <ComingSoon
      icon="🧾"
      title="Sales & POS coming to mobile"
      description="Fast checkout, barcode scanning, and digital receipts are on the way — for now, use the Dashboard to keep an eye on today's sales."
    />
  );
}
