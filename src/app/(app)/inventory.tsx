import { ComingSoon } from '@/components/coming-soon';

export default function InventoryScreen() {
  return (
    <ComingSoon
      icon="📦"
      title="Inventory coming to mobile"
      description="Product management, stock adjustments, and warehouse transfers are on the way — the Dashboard's stock health card already shows your current levels."
    />
  );
}
